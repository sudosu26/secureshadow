# secureshadow
SECURESHADOW detects security controls that silently lose their effectiveness. While traditional tools check if controls are configured correctly, SECURESHADOW checks if they still enforce the security they were designed to provide as APIs, services, and data paths change.
